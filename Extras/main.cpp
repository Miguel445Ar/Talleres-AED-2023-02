#include <iostream>
#include <memory>
#include <cstring>

namespace utils {
    void nPrintf(const char* format, ...) {
        size_t lenght = std::strlen(format);
        va_list vl;
        for(size_t i = 0; i < lenght; ++i) {

        }
    }
}

int main() {
   
    
    return 0;
}